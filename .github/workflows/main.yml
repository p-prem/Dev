name: Using env pipeline and secret pass
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Test secret
        run: echo "Secret is MY_SECRET"
        env:
          MY_SECRET: ${{ secrets.MY_SECRET }}
